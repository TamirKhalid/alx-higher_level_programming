#!/usr/bin/python3
""" if object is an instance of a class, return TRUE """


def is_same_class(obj, a_class):
    """if the object is an instance of a class, return TRUE """
    return type(obj) == a_class
